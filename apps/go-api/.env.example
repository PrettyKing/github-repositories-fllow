DATABASE_URL=postgresql://app_admin:change_me@localhost:5432/appdb?sslmode=disable
PORT=8080
GITHUB_EVENTS_TOPIC_ARN=
AWS_REGION=ap-northeast-1
# 同时配置这两项即启用 Basic Auth（保护全部 /api/*，/health 放行）；本地开发可留空。
# 云端（ECS）应由 Secrets Manager 注入，切勿把真实凭据写入镜像或仓库。
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=
