# 项目架构图

## ImageGen 完整项目架构图

![GitHub Repositories Follow 完整项目架构](./project-architecture.png)

> 该图用于方案汇报和整体介绍；下方 Mermaid 图用于版本化维护精确的拓扑、时序和职责边界。

## ImageGen AWS 巡检、事件驱动、灰度发布与 AI Ops Agent

![AWS 巡检、事件驱动、灰度发布与 AI Ops Agent](./aws-inspection-event-canary-aiops.png)

> 运维闭环图重点展示 Synthetics 巡检、SNS/SQS/DLQ、CodeDeploy Canary、告警汇聚、Agent 诊断、人审恢复、React Console 和本地 MCP。

---

## 1. 生产运行链路（总览）

![GitHub Repositories Follow 生产运行、AI Ops 与 CI/CD 完整链路总览](./production-runtime-aiops-cicd-overview-v3.png)

> ImageGen 总览图同时覆盖生产请求链路、事件与可观测性、AI Ops 人审恢复闭环，以及 Cloudflare/AWS/Lambda/ECS 的 CI/CD 灰度发布路径。

```mermaid
graph TB
    User(["👤 用户浏览器"])
    GH(["🐙 api.github.com"])
    OR(["🤖 openrouter.ai"])

    subgraph CF["☁️ Cloudflare"]
        Pages["Pages · Next.js 静态<br/>app.faithcal.xyz"]
        Redir["_redirects<br/>/api/* → api 域名"]
        CFDNS["DNS 代理<br/>api.faithcal.xyz"]
    end

    subgraph AWS["AWS · ap-northeast-1"]
        APIGW["🚪 API Gateway (HTTP API)"]
        subgraph VPC["VPC 10.0.0.0/16"]
            subgraph PUB["公有子网"]
                NAT["NAT Gateway"]
            end
            subgraph PRIV["私有子网 x2"]
                L["λ Lambda 薄代理<br/>校验 + 转发 + OpenRouter<br/>❌ 不连库"]
                ECS["🐹 ECS Fargate · Go :8080<br/>✅ 唯一连库"]
                ALB["内网 ALB · 仅测试"]
            end
            CMAP["🧭 Cloud Map<br/>go-api.&lt;stack&gt;.internal"]
            AUR[("🐘 Aurora PostgreSQL")]
        end
    end

    User -->|"HTTPS 页面"| Pages
    Pages -.->|"含 _redirects"| Redir
    User -->|"/api/* 同源"| CFDNS --> APIGW
    APIGW --> L
    L -->|"生成 bio"| OR
    L -->|"/api/* 内网发现"| CMAP --> ECS
    ECS -->|"5432"| AUR
    ECS -->|"取账户/仓库 · 经 NAT"| GH
    NAT --> GH
    ALB -.->|"VPC 内调试"| ECS

    classDef lambda fill:#FF9900,stroke:#cc7a00,color:#fff
    classDef go fill:#00ADD8,stroke:#007d9c,color:#fff
    classDef db fill:#3b6fd4,stroke:#2a4f99,color:#fff
    classDef ext fill:#f1f3f4,stroke:#888,color:#111
    class L lambda
    class ECS go
    class AUR db
    class User,GH,OR ext
```

---

## 2. 网络 / VPC 拓扑

```mermaid
graph TB
    IGW["Internet Gateway"]
    subgraph VPC["VPC 10.0.0.0/16 (DNS 开启)"]
        subgraph AZa["可用区 a"]
            Pub["公有子网 10.0.0.0/24<br/>NAT Gateway + EIP"]
            P1["私有子网 10.0.1.0/24"]
        end
        subgraph AZc["可用区 c"]
            P2["私有子网 10.0.2.0/24"]
        end
        RT1["公有路由表 0.0.0.0/0 → IGW"]
        RT2["私有路由表 0.0.0.0/0 → NAT"]
    end

    Pub -->|路由| RT1 --> IGW
    P1 --> RT2
    P2 --> RT2
    RT2 --> Pub

    L["Lambda(私有 x2)"]
    ECS["ECS Fargate(私有 x2)"]
    AUR[("Aurora(私有 x2)")]
    ALB["内网 ALB(私有 x2)"]

    P1 -.部署.-> L
    P1 -.部署.-> ECS
    P1 -.部署.-> AUR
    P1 -.部署.-> ALB

    subgraph SG["安全组规则"]
        s1["Lambda SG: 出网全放行"]
        s2["ECS SG: 入 8080 ← Lambda SG / 测试 ALB"]
        s3["DB SG: 入 5432 ← ECS SG only<br/>(Lambda 无 5432 入站 = 不连库)"]
    end

    classDef net fill:#e6f4ea,stroke:#34a853,color:#111
    class Pub,P1,P2,RT1,RT2,IGW net
```

---

## 3. CI/CD 部署流程

```mermaid
graph LR
    Dev(["开发者 · git push / 合并 main"]) --> GHA{{"Actions (push main)<br/>OIDC → AWS_DEPLOY_ROLE_ARN"}}
    PRev(["PR 事件"]) --> GHA2{{"Actions (pull_request_target)<br/>OIDC → Ops 角色"}}

    subgraph W1["deploy.yml"]
        J1["deploy: 打 Lambda 产物 → sam deploy 主栈"]
        J2["changes: 检测 go-api / ecs-fargate.yaml"]
        J3["deploy-ecs (needs deploy)<br/>buildx arm64 → ECR → cfn deploy ECS"]
    end
    subgraph W2["deploy-web.yml"]
        J4["next build → wrangler pages deploy"]
    end
    subgraph W3["preview-deploy / destroy / reconcile"]
        J5["build-only CodeBuild<br/>(内联 buildspec，仅 ECR)"]
        J6["cdk deploy/destroy per-PR 栈"]
        J7["夜间兜底清扫孤儿栈"]
    end
    subgraph W4["deploy-aiops-console.yml"]
        J8["build React/Vite + AI Ops API<br/>SAM deploy AWS → wrangler deploy Pages"]
    end

    J5 --> J6
    GHA --> J1
    GHA --> J2
    J1 --> J3
    J2 --> J3
    GHA --> J4
    GHA --> J8
    GHA2 --> J5

    J1 -->|主栈| CFN1["CFN: Lambda+VPC+Aurora"]
    J3 -->|Go 栈| CFN2["CFN: ECR+ECS+ALB+CloudMap"]
    J4 --> PAGES["Cloudflare Pages"]
    J6 --> ENV["per-PR CDK 栈 → 共享预览 ALB"]
    J8 --> AIOPSWEB["AI Ops Console<br/>Cloudflare Pages + AWS Cognito/HTTP API"]

    classDef ci fill:#6f42c1,stroke:#4c2889,color:#fff
    class GHA,GHA2,J1,J2,J3,J4,J5,J6,J7 ci
```

---

## 4. 请求时序 — 添加账户 `POST /api/github`

```mermaid
sequenceDiagram
    autonumber
    participant U as 浏览器
    participant CF as Cloudflare
    participant GW as API Gateway
    participant L as Lambda 薄代理
    participant CM as Cloud Map
    participant Go as Go (ECS)
    participant GH as api.github.com
    participant DB as Aurora

    U->>CF: POST /api/github { token }
    CF->>GW: 同源代理 (_redirects)
    GW->>L: 触发
    L->>L: Basic Auth 校验
    L->>CM: 解析 go-api.internal
    L->>Go: 转发 (透传 Authorization)
    Go->>GH: 取账户 + 翻页取仓库(≤300)
    Go->>DB: upsert 账户 + 事务内先删后插仓库
    Go-->>L: { ...row, created, reposCount, truncated }
    L-->>U: 201(新建) / 200(更新)
```

## 4b. 请求时序 — 生成个人介绍 `GET /api/profile/:username`

```mermaid
sequenceDiagram
    autonumber
    participant U as 浏览器
    participant L as Lambda 薄代理
    participant Go as Go (ECS)
    participant GH as api.github.com
    participant OR as OpenRouter

    U->>L: GET /api/profile/:username
    L->>Go: 转发
    Go->>GH: 取公开信息(不连库)
    Go-->>L: 原始 profile(无 introduction)
    alt 配了 OPENROUTER_API_KEY
        L->>OR: 用 profile 生成 bio
        OR-->>L: introduction (LLM)
    else 未配置
        L->>L: 回退模板文案
    end
    L-->>U: { ...profile, introduction }
```

---

## 5. 基于 PR 的独立预览环境

```mermaid
graph LR
    PR(["pull_request_target<br/>opened / synchronize / closed"])
    WF["Actions: preview-deploy / destroy<br/>OIDC → Ops 角色"]
    ALB["共享预览 ALB · Host 路由<br/>长期存在"]

    subgraph BUILD["构建域（不可信 PR 代码，无密钥）"]
        CB["CodeBuild build-only<br/>内联 buildspec · Build 角色仅 ECR"]
        Img["docker build arm64 · PR SHA"]
        ECR2[("ECR: pr-preview")]
    end
    subgraph DEPLOY["部署域（可信，密钥集中）"]
        Stack["CDK: PreviewPrStack · infra/cdk"]
        CFD["Cloudflare<br/>api-pr-n.faithcal.xyz"]
        GH["GitHub 展示"]
    end
    subgraph ENV["每个 PR 独立任务（私有子网，无公网 IP）"]
        GoP["go-api :8080<br/>Runtime 角色"]
        PG["postgres:16 边车<br/>随任务生死 · 不碰生产库"]
    end

    PR -->|"同仓 guard · 逻辑来自 base"| WF
    WF -->|"StartBuild 并等待"| CB
    CB --> Img
    Img --> ECR2
    WF -->|"cdk deploy/destroy"| Stack
    WF -->|"健康检查 + 增删 DNS"| CFD
    WF -->|"Summary/评论/Environment"| GH
    GoP -->|"localhost:5432"| PG
    ECR2 -.->|镜像| GoP
    Stack --> GoP
    CFD -->|"CNAME · DNS-only"| ALB
    ALB -->|"api-pr-n → 目标组"| GoP

    classDef role fill:#6f42c1,stroke:#4c2889,color:#fff
    class WF,CB role
```

**分权安全模型**：构建(不可信 PR 代码)与部署(密钥)彻底分离。
- **构建域**：CodeBuild 用**内联 buildspec**(PR 改不了步骤)，Build 角色**只能 push ECR、无任何密钥/部署权限**。
- **部署域**：`pull_request_target` 触发的**可信** workflow(逻辑来自 base 分支)经 OIDC 假设 **Ops 角色**做 cfn 部署/销毁 + 读 Cloudflare 密钥 + PassRole；**fork PR 被 same-repo guard 挡掉**。
- **三个 IAM 角色**：Ops(Actions 假设)、Build(CodeBuild，仅 ECR)、Runtime(预览任务)。清理失败(`DELETE_FAILED`)会让 destroy 工作流报错，不被吞掉。
- **稳定入口**：每 PR 任务在私有子网、无公网 IP；入口走**长期共享 ALB**(Host 路由 `api-pr-<n>` → 各 PR 目标组)，Cloudflare DNS-only CNAME 指过来 → `http://api-pr-<n>.faithcal.xyz`(无端口)。任务 IP 变化被目标组屏蔽。
- **兜底清扫**：per-PR 栈打 `Environment=preview`/`PullRequest`/`ExpiresAt` 标签；`preview-reconcile.yml` 每晚扫描，删除「PR 已关闭」或「已过期」的孤儿栈+DNS，限制成本泄漏。

---

## 6. Monorepo / 组件结构

```mermaid
graph TB
    subgraph apps["apps/"]
        web["web · Next.js 静态导出<br/>→ Cloudflare Pages"]
        server["server · Hono Lambda<br/>薄代理(校验/转发/OpenRouter)"]
        go["go-api · Go(pgx)<br/>唯一连库 + ensureSchema"]
        agent["aiops-agent · AWS Lambda<br/>编排/工具/Console API"]
        console["aiops-console · React + TypeScript<br/>Vite + Tailwind CSS"]
        mcp["aiops-mcp · 纯 stdio MCP<br/>本地 Agent Client 调用"]
    end
    subgraph packages["packages/"]
        ui["ui · shadcn/base-ui(框架无关)"]
        env["env · zod 校验(server/web)"]
        db["db · Drizzle(仅本地 migration/studio)"]
        config["config · 共享 tsconfig"]
    end
    subgraph infra["infra/ + 根"]
        tpl["template.yaml · 主栈"]
        ecs["ecs-fargate.yaml · Go 栈"]
        oidc["github-oidc.yaml · OIDC+边界"]
        prp["pr-preview*.yaml · PR 预览"]
        aio["aiops.yaml + aiops-console.yaml<br/>Agent + 管理控制台"]
    end

    web -->|import| ui
    web -->|"/api 同源"| server
    server -->|"GO_API_URL 转发"| go
    server -->|schema| env
    go -->|pgx 直连| DB[("Aurora")]
    db -.->|生成 migration| DB
    console -->|"Cognito JWT + HTTPS"| agent
    mcp -.->|"AWS SSO/Profile · 受限工具"| agent

    classDef a fill:#0969da,stroke:#0a4c9c,color:#fff
    class web,server,go a
    class agent,console,mcp a
```

---

## 7. 职责边界 / 权限模型

```mermaid
graph TB
    subgraph resp["职责边界"]
        rW["Web(Pages)<br/>只访问 API Gateway"]
        rL["Lambda<br/>校验 + 转发 + OpenRouter<br/>不访问 RDS"]
        rG["Go<br/>CRUD + 事务 + 唯一访问 PostgreSQL"]
        rC["Console/Bootstrap<br/>一次性基础设施 + IAM 边界"]
        rCI["CI/CD<br/>发布 + PR 生命周期"]
    end
    subgraph iam["IAM(OIDC + 权限边界，防提权)"]
        oidc["GitHub OIDC Provider"]
        deploy["DeployRole<br/>限本区域/栈前缀"]
        lb["Lambda 权限边界"]
        eb["ECS 权限边界"]
        trig["PR Trigger 角色(仅 StartBuild)"]
        pr["lambda / ecs-tasks"]
    end

    oidc --> deploy
    oidc --> trig
    deploy -->|"建角色须挂"| lb
    deploy -->|"建角色须挂"| eb
    deploy -->|"PassRole 限"| pr
    rCI --> deploy

    classDef sec fill:#cf222e,stroke:#a40e26,color:#fff
    class oidc,deploy,lb,eb,trig sec
```

---

## 8. AI Ops Console 与本地 MCP

```mermaid
flowchart LR
    Admin(["运维管理员"])
    Client(["Codex / Claude Desktop"])

    subgraph WebConsole["独立 Web 控制台 · apps/aiops-console"]
        React["React 19 + TypeScript<br/>Vite + Tailwind CSS"]
        Mock["本地预览模式<br/>Mock 数据 · 不访问 AWS"]
    end

    subgraph Edge["混合托管入口"]
        Pages["Cloudflare Pages<br/>React 静态资源"]
        Cognito["Cognito User Pool<br/>OAuth2 Code + PKCE"]
        APIGW["HTTP API<br/>JWT Authorizer"]
    end

    subgraph Ops["AI Ops 服务边界"]
        ConsoleFn["Console API Lambda<br/>只读查询 + 安全队列测试"]
        Orchestrator["Orchestrator Lambda"]
        Agent["Bedrock Agent<br/>受限 Action Tools"]
        Incident[("DynamoDB Incident")]
    end

    subgraph Signals["项目限定 AWS 信号"]
        CW["CloudWatch Alarms / Logs"]
        Syn["Synthetics Canary"]
        Queue["SNS / SQS / DLQ"]
        Deploy["CodeDeploy"]
    end

    subgraph Local["本地 MCP · apps/aiops-mcp"]
        MCP["stdio MCP Server<br/>无 HTTP · 无前端 · 无监听端口"]
        SSO["AWS SSO / Profile<br/>临时最小权限凭据"]
    end

    Admin -->|"HTTPS"| Pages --> React
    React -->|"登录/退出"| Cognito
    React -->|"Bearer JWT"| APIGW --> ConsoleFn
    React -.->|"仅 pnpm dev"| Mock
    ConsoleFn --> CW
    ConsoleFn --> Syn
    ConsoleFn --> Queue
    ConsoleFn --> Deploy
    ConsoleFn --> Incident

    CW -->|"Alarm Event"| Orchestrator --> Agent --> Incident
    Client <-->|"MCP stdio"| MCP --> SSO
    SSO --> CW
    SSO --> Syn
    SSO --> Queue
    SSO --> Deploy
    MCP -->|"写操作仅创建审批申请"| Orchestrator

    classDef web fill:#087ea4,stroke:#055d78,color:#fff
    classDef aws fill:#ff9900,stroke:#cc7a00,color:#fff
    classDef local fill:#6f42c1,stroke:#4c2889,color:#fff
    class React,Mock,Pages web
    class Cognito,APIGW,ConsoleFn,Orchestrator,Agent,Incident,CW,Syn,Queue,Deploy aws
    class MCP,SSO local
```

### 边界说明

- `aiops-console` 是独立的 React 管理控制台，不属于 `aiops-mcp`，也不向浏览器下发 AWS 凭据。
- 线上静态资源由 Cloudflare Pages 托管；AWS Cognito 完成管理员认证，HTTP API JWT Authorizer 保护 Console API。
- 本地 `pnpm --filter aiops-console dev` 默认使用安全 Mock 数据，端口为 `4322`；不会访问或修改 AWS。
- `aiops-mcp` 只使用 stdio 与 MCP Client 通信，没有 Web 页面、HTTP Server 或固定端口。
- Console API 和 MCP 都只能访问环境 allowlist 内的告警、日志组、Canary、队列和部署组；恢复类写操作仍需审批。
